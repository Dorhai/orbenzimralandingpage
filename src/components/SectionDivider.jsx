/**
 * Thin two-tone horizontal rule between page sections (red brand variant).
 */
export default function SectionDivider() {
  return (
    <div
      className="section-divider w-full shrink-0"
      role="separator"
      aria-hidden="true"
    />
  );
}
